name: Regenerate Issue 19 lockfile

on:
  pull_request:
    branches:
      - main

permissions:
  contents: write

jobs:
  regenerate:
    if: github.head_ref == '19-add-prettier-config'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Set up npm 11.17.0
        run: npm install --global npm@11.17.0

      - name: Regenerate lockfile and format files
        working-directory: app
        run: |
          rm -rf node_modules package-lock.json
          npm install
          npm run format
          npm run format:check

      - name: Commit generated files
        run: |
          rm .github/workflows/issue-19-regenerate-lock.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "chore: regenerate package lock and format files"
          git push origin HEAD:${{ github.head_ref }}
